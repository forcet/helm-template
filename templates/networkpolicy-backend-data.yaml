{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: "{{ .Values.name }}-backend-data"
  namespace: {{ .Values.namespace }}
spec:
  podSelector:
    matchLabels:
      app: {{ .Values.networkPolicy.backendData.app }}
  policyTypes:
    - Ingress
    - Egress

  ingress:
    - from:
        {{- range .Values.networkPolicy.backendData.allowedFromApps }}
        - podSelector:
            matchLabels:
              app: {{ . }}
        {{- end }}

  egress:
    - to:
        {{- range .Values.networkPolicy.backendData.allowedTo }}
        - podSelector:
            matchLabels:
              app: {{ .app }}
        {{- end }}
      ports:
        {{- range .Values.networkPolicy.backendData.allowedTo }}
        - protocol: TCP
          port: {{ .port }}
        {{- end }}
{{- end }}